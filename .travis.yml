language: node_js
node_js:
  - "stable"
env:
  - NODE_ENV=test
services:
  - mysql
before_install:
  - sudo mysql -e "use mysql; update user set authentication_string=PASSWORD('kobeBryant_24') where User='root'; update user set plugin='mysql_native_password';FLUSH PRIVILEGES;"
before_script:
  - sudo mysql -e 'CREATE DATABASE turing_test;'
  - chmod 0777 ./node_modules/.bin/mocha
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build
script:
  - npm test
cache:
  directories:
    - "node_modules"
